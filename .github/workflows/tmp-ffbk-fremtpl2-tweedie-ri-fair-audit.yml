name: FFBK freMTPL2 Tweedie RI fair-score audit

on:
  push:
    branches:
      - research/fremtpl2-tweedie-ri-20260908-sol56
  workflow_dispatch:

permissions:
  contents: read

jobs:
  execute:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install frozen runtime
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy==2.5.3 pandas==3.0.5 scipy==1.18.1 scikit-learn==1.9.0 rdata==1.1.0
      - name: Compile audit scripts
        run: |
          python -m py_compile scripts/tmp_ffbk_fremtpl2_aggregate_law.py scripts/tmp_ffbk_fremtpl2_tweedie_ri.py scripts/tmp_ffbk_fremtpl2_tweedie_ri_fair_audit.py
      - name: Execute fixed-law fair-score audit
        run: python scripts/tmp_ffbk_fremtpl2_tweedie_ri_fair_audit.py
      - name: Upload exact fair-audit receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ffbk-fremtpl2-tweedie-ri-fair-audit-result
          path: tmp_ffbk_fremtpl2_tweedie_ri_fair_audit_result.json
          if-no-files-found: error
          retention-days: 90
