name: tmp-ffbk-exact-tail-law

on:
  push:
    branches:
      - research/fremtpl2-exact-tail-law-20260908
    paths:
      - scripts/tmp_ffbk_fremtpl2_exact_tail_law.py
      - .github/workflows/tmp-ffbk-exact-tail-law.yml

permissions:
  contents: read

jobs:
  run-study:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install frozen research dependencies
        run: >-
          python -m pip install --disable-pip-version-check
          numpy==2.5.3 pandas==3.0.5 scipy==1.18.1
          scikit-learn==1.9.0 rdata==1.1.0
      - name: Run exact compound-tail law audit
        run: python scripts/tmp_ffbk_fremtpl2_exact_tail_law.py
      - name: Upload result receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ffbk-exact-tail-law-result
          path: tmp_ffbk_fremtpl2_exact_tail_law_result.json
          if-no-files-found: error
